object Test {
  def main(args: Array[String]): Unit = {
    // val m = Map.empty[Int, List[String]]
    // m.map { case (k, v) => ??? }

    val tt = (Int, List.empty[String])

    val l: Int = tt._1
  }
}
